import UIKit

// You can place ant type of variable inside your string using \
// followed by your variable name in paranthesses

var score = 85
var str = "Your score was \(score)"

// You can make strings out of strings

var result = "The test results are here: \(str)"

var firstNumber = 3
var secondNumber = 4

var addition = "\(firstNumber) + \(secondNumber) = \(3 + 4)"
